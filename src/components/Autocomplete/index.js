import React, { Component } from 'react';

const Autocomplete = () => (
  <div className="auto-complete">
    <input type="search" placeholder="Seek your pokemon" />
  </div>
)

export default Autocomplete;
